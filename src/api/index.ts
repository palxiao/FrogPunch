import * as exHome from './home';
import * as exNote from './note';
import * as exEvent from './event';
import * as exUser from './user';
import * as exTarget from './target';
import * as exGql from './gql';

export const home = exHome
export const gql = exGql
export const note = exNote
export const event = exEvent
export const user = exUser
export const target = exTarget
